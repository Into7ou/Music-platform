<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.intomusic.musicplatform.modules.user.mapper.UserMapper">

    <select id="selectByUsername" resultType="User">
        SELECT id, username, password, nickname, avatar, bio, create_time
        FROM sys_user
        WHERE username = #{username}
    </select>

    <select id="selectById" resultType="User">
        SELECT id, username, password, nickname, avatar, bio, create_time
        FROM sys_user
        WHERE id = #{id}
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO sys_user (username, password, nickname, avatar, create_time)
        VALUES (#{username}, #{password}, #{nickname}, #{avatar}, NOW())
    </insert>

    <update id="update">
        UPDATE sys_user
        <set>
            <if test="nickname != null and nickname != ''">
                nickname = #{nickname},
            </if>
            <if test="avatar != null">
                avatar = #{avatar},
            </if>
            <if test="bio != null">
                bio = #{bio},
            </if>
        </set>
        WHERE id = #{id}
    </update>

    <update id="updateAvatar">
        UPDATE sys_user
        SET avatar = #{avatar}
        WHERE id = #{id}
    </update>

    <update id="updateBio">
        UPDATE sys_user
        SET bio = #{bio}
        WHERE id = #{id}
    </update>

    <!-- ========== 管理员功能相关 ========== -->

    <!-- 查询所有用户（分页，支持条件筛选） -->
    <select id="selectAllUsers" resultType="User">
        SELECT id, username, nickname, avatar, bio, role_id, status, create_time, update_time
        FROM sys_user
        <where>
            <if test="username != null and username != ''">
                AND username LIKE CONCAT('%', #{username}, '%')
            </if>
            <if test="roleId != null">
                AND role_id = #{roleId}
            </if>
            <if test="status != null">
                AND status = #{status}
            </if>
        </where>
        ORDER BY create_time DESC
        LIMIT #{offset}, #{limit}
    </select>

    <!-- 查询用户总数（支持条件筛选） -->
    <select id="countAllUsers" resultType="Long">
        SELECT COUNT(*)
        FROM sys_user
        <where>
            <if test="username != null and username != ''">
                AND username LIKE CONCAT('%', #{username}, '%')
            </if>
            <if test="roleId != null">
                AND role_id = #{roleId}
            </if>
            <if test="status != null">
                AND status = #{status}
            </if>
        </where>
    </select>

    <!-- 更新用户状态 -->
    <update id="updateUserStatus">
        UPDATE sys_user
        SET status = #{status}
        WHERE id = #{id}
    </update>

    <!-- 更新用户角色 -->
    <update id="updateUserRole">
        UPDATE sys_user
        SET role_id = #{roleId}
        WHERE id = #{id}
    </update>

</mapper>