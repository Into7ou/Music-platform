<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.intomusic.musicplatform.modules.admin.mapper.RoleMapper">

    <!-- 结果映射 -->
    <resultMap id="RoleResultMap" type="com.intomusic.musicplatform.modules.admin.model.entity.Role">
        <id column="id" property="id"/>
        <result column="role_name" property="roleName"/>
        <result column="role_code" property="roleCode"/>
        <result column="description" property="description"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <!-- 根据ID查询角色 -->
    <select id="selectById" resultMap="RoleResultMap">
        SELECT id, role_name, role_code, description, create_time, update_time
        FROM sys_role
        WHERE id = #{id}
    </select>

    <!-- 根据角色编码查询角色 -->
    <select id="selectByRoleCode" resultMap="RoleResultMap">
        SELECT id, role_name, role_code, description, create_time, update_time
        FROM sys_role
        WHERE role_code = #{roleCode}
    </select>

    <!-- 查询所有角色 -->
    <select id="selectAll" resultMap="RoleResultMap">
        SELECT id, role_name, role_code, description, create_time, update_time
        FROM sys_role
        ORDER BY id ASC
    </select>

</mapper>